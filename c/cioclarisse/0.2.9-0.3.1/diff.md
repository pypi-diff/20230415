# Comparing `tmp/cioclarisse-0.2.9-py2.py3-none-any.whl.zip` & `tmp/cioclarisse-0.3.1-py2.py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,35 +1,35 @@
-Zip file size: 47111 bytes, number of entries: 33
--rw-r--r--  2.0 unx        5 b- defN 22-Jul-04 21:03 cioclarisse/VERSION
--rw-r--r--  2.0 unx        0 b- defN 22-Jul-04 21:02 cioclarisse/__init__.py
--rw-r--r--  2.0 unx     5023 b- defN 22-Jul-04 21:02 cioclarisse/attr_docs.py
--rw-r--r--  2.0 unx     2537 b- defN 22-Jul-04 21:02 cioclarisse/clarisse_config.py
--rw-r--r--  2.0 unx    15214 b- defN 22-Jul-04 21:02 cioclarisse/conductor_job.py
--rw-r--r--  2.0 unx       35 b- defN 22-Jul-04 21:02 cioclarisse/const.py
--rw-r--r--  2.0 unx     1533 b- defN 22-Jul-04 21:02 cioclarisse/debug_ui.py
--rw-r--r--  2.0 unx     6303 b- defN 22-Jul-04 21:02 cioclarisse/dependencies.py
--rw-r--r--  2.0 unx     8624 b- defN 22-Jul-04 21:02 cioclarisse/environment_ui.py
--rw-r--r--  2.0 unx     9754 b- defN 22-Jul-04 21:02 cioclarisse/extra_uploads_ui.py
--rw-r--r--  2.0 unx     6132 b- defN 22-Jul-04 21:02 cioclarisse/frames_ui.py
--rw-r--r--  2.0 unx     1110 b- defN 22-Jul-04 21:02 cioclarisse/instances_ui.py
--rw-r--r--  2.0 unx     1001 b- defN 22-Jul-04 21:02 cioclarisse/notifications_ui.py
--rw-r--r--  2.0 unx     2705 b- defN 22-Jul-04 21:02 cioclarisse/post_install.py
--rw-r--r--  2.0 unx     2628 b- defN 22-Jul-04 21:02 cioclarisse/preview_ui.py
--rw-r--r--  2.0 unx     1461 b- defN 22-Jul-04 21:02 cioclarisse/projects_ui.py
--rw-r--r--  2.0 unx     1858 b- defN 22-Jul-04 21:02 cioclarisse/refresh.py
--rw-r--r--  2.0 unx     1077 b- defN 22-Jul-04 21:02 cioclarisse/reloader.py
--rw-r--r--  2.0 unx      930 b- defN 22-Jul-04 21:02 cioclarisse/software_ui.py
--rw-r--r--  2.0 unx      751 b- defN 22-Jul-04 21:02 cioclarisse/startup.py
--rw-r--r--  2.0 unx    19815 b- defN 22-Jul-04 21:02 cioclarisse/submission.py
--rw-r--r--  2.0 unx     2215 b- defN 22-Jul-04 21:02 cioclarisse/submit_actions.py
--rw-r--r--  2.0 unx     3091 b- defN 22-Jul-04 21:02 cioclarisse/task.py
--rw-r--r--  2.0 unx      511 b- defN 22-Jul-04 21:02 cioclarisse/upload_ui.py
--rw-r--r--  2.0 unx     2900 b- defN 22-Jul-04 21:02 cioclarisse/utils.py
--rw-r--r--  2.0 unx     8422 b- defN 22-Jul-04 21:02 cioclarisse/validation.py
--rw-r--r--  2.0 unx     3986 b- defN 22-Jul-04 21:02 cioclarisse/validation_ui.py
--rwxr-xr-x  2.0 unx     3711 b- defN 22-Jul-04 21:02 cioclarisse/scripts/ciocnode
--rw-r--r--  2.0 unx     2095 b- defN 22-Jul-04 21:02 cioclarisse/scripts/cioprep.py
--rw-r--r--  2.0 unx    10525 b- defN 22-Jul-04 21:03 cioclarisse-0.2.9.dist-info/METADATA
--rw-r--r--  2.0 unx      110 b- defN 22-Jul-04 21:03 cioclarisse-0.2.9.dist-info/WHEEL
--rw-r--r--  2.0 unx       12 b- defN 22-Jul-04 21:03 cioclarisse-0.2.9.dist-info/top_level.txt
-?rw-rw-r--  2.0 unx     2695 b- defN 22-Jul-04 21:03 cioclarisse-0.2.9.dist-info/RECORD
-33 files, 128769 bytes uncompressed, 42823 bytes compressed:  66.7%
+Zip file size: 49913 bytes, number of entries: 33
+-rw-r--r--  2.0 unx        5 b- defN 23-Apr-15 16:24 cioclarisse/VERSION
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-15 16:24 cioclarisse/__init__.py
+-rw-r--r--  2.0 unx     4778 b- defN 23-Apr-15 16:24 cioclarisse/attr_docs.py
+-rw-r--r--  2.0 unx     2537 b- defN 23-Apr-15 16:24 cioclarisse/clarisse_config.py
+-rw-r--r--  2.0 unx    16057 b- defN 23-Apr-15 16:24 cioclarisse/conductor_job.py
+-rw-r--r--  2.0 unx       35 b- defN 23-Apr-15 16:24 cioclarisse/const.py
+-rw-r--r--  2.0 unx     1533 b- defN 23-Apr-15 16:24 cioclarisse/debug_ui.py
+-rw-r--r--  2.0 unx     6303 b- defN 23-Apr-15 16:24 cioclarisse/dependencies.py
+-rw-r--r--  2.0 unx     8371 b- defN 23-Apr-15 16:24 cioclarisse/environment_ui.py
+-rw-r--r--  2.0 unx     9754 b- defN 23-Apr-15 16:24 cioclarisse/extra_uploads_ui.py
+-rw-r--r--  2.0 unx     6132 b- defN 23-Apr-15 16:24 cioclarisse/frames_ui.py
+-rw-r--r--  2.0 unx     1110 b- defN 23-Apr-15 16:24 cioclarisse/instances_ui.py
+-rw-r--r--  2.0 unx     8032 b- defN 23-Apr-15 16:24 cioclarisse/metadata_ui.py
+-rw-r--r--  2.0 unx     1001 b- defN 23-Apr-15 16:24 cioclarisse/notifications_ui.py
+-rw-r--r--  2.0 unx     2705 b- defN 23-Apr-15 16:24 cioclarisse/post_install.py
+-rw-r--r--  2.0 unx     2628 b- defN 23-Apr-15 16:24 cioclarisse/preview_ui.py
+-rw-r--r--  2.0 unx     1461 b- defN 23-Apr-15 16:24 cioclarisse/projects_ui.py
+-rw-r--r--  2.0 unx     1858 b- defN 23-Apr-15 16:24 cioclarisse/refresh.py
+-rw-r--r--  2.0 unx     1052 b- defN 23-Apr-15 16:24 cioclarisse/reloader.py
+-rw-r--r--  2.0 unx      930 b- defN 23-Apr-15 16:24 cioclarisse/software_ui.py
+-rw-r--r--  2.0 unx      751 b- defN 23-Apr-15 16:24 cioclarisse/startup.py
+-rw-r--r--  2.0 unx    20592 b- defN 23-Apr-15 16:24 cioclarisse/submission.py
+-rw-r--r--  2.0 unx     2215 b- defN 23-Apr-15 16:24 cioclarisse/submit_actions.py
+-rw-r--r--  2.0 unx     3149 b- defN 23-Apr-15 16:24 cioclarisse/task.py
+-rw-r--r--  2.0 unx     3271 b- defN 23-Apr-15 16:24 cioclarisse/utils.py
+-rw-r--r--  2.0 unx     8572 b- defN 23-Apr-15 16:24 cioclarisse/validation.py
+-rw-r--r--  2.0 unx     3986 b- defN 23-Apr-15 16:24 cioclarisse/validation_ui.py
+-rwxr-xr-x  2.0 unx     3711 b- defN 23-Apr-15 16:24 cioclarisse/scripts/ciocnode
+-rw-r--r--  2.0 unx     2095 b- defN 23-Apr-15 16:24 cioclarisse/scripts/cioprep.py
+-rw-r--r--  2.0 unx    11061 b- defN 23-Apr-15 16:24 cioclarisse-0.3.1.dist-info/METADATA
+-rw-r--r--  2.0 unx      110 b- defN 23-Apr-15 16:24 cioclarisse-0.3.1.dist-info/WHEEL
+-rw-r--r--  2.0 unx       12 b- defN 23-Apr-15 16:24 cioclarisse-0.3.1.dist-info/top_level.txt
+?rw-rw-r--  2.0 unx     2698 b- defN 23-Apr-15 16:24 cioclarisse-0.3.1.dist-info/RECORD
+33 files, 138505 bytes uncompressed, 45621 bytes compressed:  67.1%
```

## zipnote {}

```diff
@@ -30,14 +30,17 @@
 
 Filename: cioclarisse/frames_ui.py
 Comment: 
 
 Filename: cioclarisse/instances_ui.py
 Comment: 
 
+Filename: cioclarisse/metadata_ui.py
+Comment: 
+
 Filename: cioclarisse/notifications_ui.py
 Comment: 
 
 Filename: cioclarisse/post_install.py
 Comment: 
 
 Filename: cioclarisse/preview_ui.py
@@ -63,17 +66,14 @@
 
 Filename: cioclarisse/submit_actions.py
 Comment: 
 
 Filename: cioclarisse/task.py
 Comment: 
 
-Filename: cioclarisse/upload_ui.py
-Comment: 
-
 Filename: cioclarisse/utils.py
 Comment: 
 
 Filename: cioclarisse/validation.py
 Comment: 
 
 Filename: cioclarisse/validation_ui.py
@@ -81,20 +81,20 @@
 
 Filename: cioclarisse/scripts/ciocnode
 Comment: 
 
 Filename: cioclarisse/scripts/cioprep.py
 Comment: 
 
-Filename: cioclarisse-0.2.9.dist-info/METADATA
+Filename: cioclarisse-0.3.1.dist-info/METADATA
 Comment: 
 
-Filename: cioclarisse-0.2.9.dist-info/WHEEL
+Filename: cioclarisse-0.3.1.dist-info/WHEEL
 Comment: 
 
-Filename: cioclarisse-0.2.9.dist-info/top_level.txt
+Filename: cioclarisse-0.3.1.dist-info/top_level.txt
 Comment: 
 
-Filename: cioclarisse-0.2.9.dist-info/RECORD
+Filename: cioclarisse-0.3.1.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## cioclarisse/VERSION

```diff
@@ -1 +1 @@
-0.2.9
+0.3.1
```

## cioclarisse/attr_docs.py

```diff
@@ -107,19 +107,14 @@
 
     s_class.set_attr_doc(
         "localize_contexts",
         """Localize contexts before dispatching to Conductor. This will cause your project to be saved and then reloaded after submission, which may take a long time for large projects.""",
     )
 
     s_class.set_attr_doc(
-        "clean_up_render_package",
-        """Removes the render project file after submission. If local_upload is switched off, then this attribute will be hidden and the render project will not be removed.""",
-    )
-
-    s_class.set_attr_doc(
         "notify",
         """Indicates that notifications will be sent by email on job completion.""",
     )
 
     s_class.set_attr_doc(
         "email_addresses",
         """A comma delimited list of emails addresses to notify on job completion.""",
```

## cioclarisse/conductor_job.py

```diff
@@ -5,31 +5,33 @@
 import traceback
 
 import ix
 
 try:
     from importlib import reload 
 except ImportError:
-    import reload
+    from imp import reload
+except ImportError:
+    pass
     
 
 from cioclarisse.utils import ConductorError
 from cioclarisse import reloader
 from cioclarisse import const as k
 from cioclarisse import (
     attr_docs,
     debug_ui,
     environment_ui,
+    metadata_ui,
     extra_uploads_ui,
     frames_ui,
     notifications_ui,
     projects_ui,
     refresh,
-    submit_actions,
-    upload_ui
+    submit_actions
 
 )
 
 import ciocore.loggeria
 
 # Setup logging
 ciocore.loggeria.setup_conductor_logging(
@@ -69,14 +71,16 @@
         # crashes if an exception is thrown from a scripted class. See
         # https://www.isotropix.com/user/bugtracker/363 .
         try:
             if action_name == "manage_extra_uploads":
                 extra_uploads_ui.build(obj, data)
             elif action_name == "manage_extra_environment":
                 environment_ui.build(obj, data)
+            elif action_name == "manage_metadata":
+                metadata_ui.build(obj, data)
             elif action_name == "preview":
                 refresh.refresh(obj)
                 submit_actions.preview(obj, data)
             elif action_name == "write_render_package":
                 refresh.refresh(obj)
                 submit_actions.write_render_package(obj)
             elif action_name == "submit":
@@ -122,16 +126,14 @@
                 frames_ui.handle_images(obj, attr)
             elif attr_name == "notify":
                 notifications_ui.notify_changed(obj, attr)
             elif attr_name == "email_addresses":
                 notifications_ui.handle_email_addresses(obj, attr)
             elif attr_name == "conductor_log_level":
                 debug_ui.handle_log_level(obj, attr)
-            elif attr_name == "local_upload":
-                upload_ui.handle_local_upload(obj, attr)
             else:
                 pass
         except (BaseException, ValueError, AttributeError, ConductorError) as ex:
             if verbose_errors:
                 ix.log_error(traceback.format_exc())
             else:
                 ix.log_error(str(ex))
@@ -145,14 +147,15 @@
         """
         self.declare_actions(s_class)
         self.declare_general_attributes(s_class)
         self.declare_frames_attributes(s_class)
         self.declare_machines_attributes(s_class)
         self.declare_upload_attributes(s_class)
         self.declare_environment_attributes(s_class)
+        self.declare_metadata_attributes(s_class)
         self.declare_task_attributes(s_class)
         self.declare_notification_attributes(s_class)
         self.declare_dev_attributes(s_class)
 
         attr_docs.set_docs(s_class)
 
     def declare_dev_attributes(self, s_class):
@@ -187,14 +190,23 @@
             ix.api.OfAttr.TYPE_BOOL,
             ix.api.OfAttr.CONTAINER_SINGLE,
             ix.api.OfAttr.VISUAL_HINT_DEFAULT,
             "debug",
         )
         attr.set_bool(False)
 
+        attr = s_class.add_attribute(
+            "disable_validation",
+            ix.api.OfAttr.TYPE_BOOL,
+            ix.api.OfAttr.CONTAINER_SINGLE,
+            ix.api.OfAttr.VISUAL_HINT_DEFAULT,
+            "debug",
+        )
+        attr.set_bool(False)
+
     def declare_actions(self, s_class):
         """
         Creates connect, preview, and submit buttons.
 
         Args:
             s_class (ScriptedClass):  The scripted class.
         """
@@ -452,14 +464,33 @@
             ix.api.OfAttr.TYPE_STRING,
             ix.api.OfAttr.CONTAINER_LIST,
             ix.api.OfAttr.VISUAL_HINT_DEFAULT,
             "environment",
         )
         attr.set_read_only(True)
 
+    def declare_metadata_attributes(self, s_class):
+        """
+        Sets up metadata.
+
+        Args:
+            s_class (ScriptedClass):  The scripted class.
+        """
+        self.add_action(s_class, "manage_metadata", "metadata")
+
+        attr = s_class.add_attribute(
+            "metadata",
+            ix.api.OfAttr.TYPE_STRING,
+            ix.api.OfAttr.CONTAINER_LIST,
+            ix.api.OfAttr.VISUAL_HINT_DEFAULT,
+            "metadata",
+        )
+        attr.set_read_only(True)
+
+
     def declare_notification_attributes(self, s_class):
         """
         Sets email addresses to be notified on job completion.
 
         Args:
             s_class (ScriptedClass):  The scripted class.
         """
```

## cioclarisse/environment_ui.py

```diff
@@ -16,15 +16,15 @@
 HEIGHT = 300
 WIDTH = 900
 PADDING = 5
 
 SYMBOL_BUT_WIDTH = 30
 CHECKBOX_WIDTH = 50
 
-BOTTOM_BUT_WIDTH = WIDTH // 3
+BOTTOM_BUT_WIDTH = WIDTH // 2
 VAR_WDG_WIDTH = 300
 VAL_WDG_WIDTH = WIDTH - (
     SYMBOL_BUT_WIDTH + CHECKBOX_WIDTH + VAR_WDG_WIDTH + (PADDING * 5)
 )
 
 
 class VarWidget(ix.api.GuiWidget):
@@ -206,19 +206,14 @@
         current_y += HEIGHT
 
         self.close_but = ix.api.GuiPushButton(
             self, 0, current_y, BOTTOM_BUT_WIDTH, BTN_HEIGHT, "Close"
         )
         self.connect(self.close_but, "EVT_ID_PUSH_BUTTON_CLICK", self.on_close_but)
 
-        self.apply_but = ix.api.GuiPushButton(
-            self, BOTTOM_BUT_WIDTH, current_y, BOTTOM_BUT_WIDTH, BTN_HEIGHT, "Apply"
-        )
-        self.connect(self.apply_but, "EVT_ID_PUSH_BUTTON_CLICK", self.on_apply_but)
-
         self.go_but = ix.api.GuiPushButton(
             self,
             (WIDTH - BOTTOM_BUT_WIDTH),
             current_y,
             BOTTOM_BUT_WIDTH,
             BTN_HEIGHT,
             "Go",
@@ -235,19 +230,14 @@
 
     def on_close_but(self, sender, eventid):
         """
         Hides only.
         """
         self.hide()
 
-    def on_apply_but(self, sender, eventid):
-        """
-        Saves values on the attribute and keep the window visible.
-        """
-        self._apply()
 
     def on_go_but(self, sender, eventid):
         """
         Saves values on the attribute and hide(destroy) the window.
         """
         self._apply()
         self.hide()
@@ -274,16 +264,22 @@
     Listen for events until the window is hidden.
     """
     node = args[0]
     win = EnvWindow(node)
 
     attr = node.get_attribute("extra_environment")
     json_entries = ix.api.CoreStringArray()
-    attr.get_values(json_entries)
-    entries = [json.loads(entry) for entry in json_entries]
+    attr.get_raw_values(json_entries)
+    entries = []
+    for entry in json_entries:
+        try:
+            r = json.loads(entry)
+            entries.append(json.loads(entry))
+        except BaseException:
+            pass
     if not entries:
         entries = [{}]
     win.panel.add_entries(*entries)
 
     win.show_modal()
     while win.is_shown():
         ix.application.check_for_events()
```

## cioclarisse/reloader.py

```diff
@@ -7,50 +7,49 @@
 It reloads files imported by ConductorJob. Not ConductorJob itself.
 """
 try:
     from importlib import reload 
 except ImportError:
     from imp import reload
 except ImportError:
-    import reload
+    pass
     
 
 from cioclarisse import utils, clarisse_config
 from cioclarisse import (
     attr_docs,
     debug_ui,
     dependencies,
     validation,
     environment_ui,
     extra_uploads_ui,
+    metadata_ui,
     frames_ui,
     instances_ui,
     validation_ui,
     notifications_ui,
     preview_ui,
     projects_ui,
     refresh,
     submission,
     submit_actions,
-    task,
-    upload_ui,
+    task
 )
  
 reload(utils)
 reload(clarisse_config)
 reload(refresh)
 reload(dependencies)
 reload(debug_ui)
 reload(validation)
 reload(environment_ui)
 reload(extra_uploads_ui)
+reload(metadata_ui)
 reload(validation_ui)
 reload(frames_ui)
 reload(instances_ui)
 reload(task)
 reload(notifications_ui)
 reload(projects_ui)
 reload(submission)
 reload(submit_actions)
-reload(preview_ui)
-reload(upload_ui)
 reload(attr_docs)
```

## cioclarisse/submission.py

```diff
@@ -56,56 +56,52 @@
         self.node = node
         self.project_filename = ix.application.get_current_project_filename()
         self.tmpdir = Path(utils.conductor_temp_dir())
 
         self.render_package_path = self._get_render_package_path()
         self.local_upload = self.node.get_attribute("local_upload").get_bool()
 
-
         self.project = self._get_project()
         self.notifications = self._get_notifications()
 
         self.sequence = self._get_sequence()
         self.sources = self._get_sources()
         self.instance = self._get_instance()
 
         out = self._get_output_directory()
         self.common_output_path = out["common_path"]
         self.output_paths = out["output_paths"]
 
-
         tile_width = int(self.node.get_attribute("tiles").get_long())
         self.tiles = tile_width * tile_width
 
         sw = self._get_software()
         self.environment = sw["env"]
         self.package_ids = sw["ids"]
-
+        
 
         self.dependencies = deps.collect(self.node)
         self.dependencies.add(self.render_package_path)
- 
 
         self.tokens = self.get_context()
         expander = Expander(**self.tokens)
+        
         self.title = expander.evaluate(self.node.get_attribute("title").get_string())
+        self.metadata = self._get_metadata(expander)
 
- 
         self.tasks = []
 
         task_att = self.node.get_attribute("task_template")
         for chunk in self.sequence["main"].chunks():
             for tile_number in range(1, self.tiles + 1):
                 tile_spec = (self.tiles, tile_number)
                 self.tasks.append(
                     Task(chunk, task_att, self.sources, tile_spec, self.tokens)
                 )
 
-
-
     def _get_sources(self):
         """
         Get the images/layers, along with associated Sequence objects.
 
         If we are not rendering a custom range, then the sequence for
         each image may be different.
 
@@ -232,15 +228,14 @@
         )
         tokens["renderpackage"] = tokens["render_package"] 
 
         tokens["project"] = self.project 
 
         main_seq = self.sequence["main"]
         scout_seq = self.sequence["scout"]
-
         tokens["scout"] = str(scout_seq)
         tokens["chunksize"] = str(main_seq.chunk_size)
         tokens["chunkcount"] = str(main_seq.chunk_count())
         tokens["scoutcount"] = str(len(scout_seq or []))
         tokens["sequencelength"] = str(len(main_seq))
         tokens["sequence"] = str(main_seq)
         tokens["sequencemin"] = str(main_seq.start)
@@ -351,14 +346,37 @@
         result["ids"].append(host_package["package_id"])
         result["env"].extend(host_package)
         result["env"].extend(self._get_extra_env_vars())
 
         return result
 
 
+    def _get_metadata(self, expander):
+        """Resolve metadata entries
+
+        Args:
+            expander (_type_): _description_
+
+        Returns:
+            _type_: _description_
+        """
+        result = {}
+        json_entries = ix.api.CoreStringArray()
+        self.node.get_attribute("metadata").get_raw_values(json_entries)
+
+        exist = False
+        for entry in [json.loads(j) for j in json_entries]:
+            if entry["key"] and entry["value"]:
+                result[entry["key"]] = entry["value"]
+                exist = True
+        if not exist:
+            return None
+        return expander.evaluate(result)
+
+
     def _get_extra_env_vars(self):
         """
         Collect any environment specified by the user.
 
         Returns:
             list of dict: name, value and merge policy of user specified env vars.
         """
@@ -408,14 +426,16 @@
         result["output_path"] = self.common_output_path.fslash()
         result["chunk_size"] = self.sequence["main"].chunk_size
         result["instance_type"] = self.instance["name"]
 
         result["tasks_data"] = [task.data() for task in self.tasks]
 
         result["job_title"] = self.title
+        if self.metadata:
+            result["metadata"] = self.metadata
   
         return result
 
     def submit(self):
         """
         Submit all jobs.
```

## cioclarisse/task.py

```diff
@@ -56,17 +56,17 @@
         tokens = {}
 
         chunks = []
         image_names = []
         for source in self.sources:
             intersection = self.chunk.intersection(source["sequence"])
             if intersection:
-                chunks.append(intersection.to(":", "%", "; "))
+                chunks.append(intersection.to(":", "%", ";"))
                 image_names.append(source["image"].get_full_name())
-
+        chunks = ["'{}'".format(chunk) for chunk in chunks]
         tokens["tile_number"] = str(self.tile_spec[1])
         tokens["chunks"] = " ".join(chunks)
         tokens["sources"] = " ".join(image_names)
         tokens["chunklength"] = str(len(self.chunk))
         tokens["chunkstart"] = str(self.chunk.start)
         tokens["chunkend"] = str(self.chunk.end)
```

## cioclarisse/utils.py

```diff
@@ -64,23 +64,29 @@
     temp_path = tempfile.mktemp()
 
     shutil.copy2(filename, temp_path)
     os.remove(filename)
     with open(filename, "w+") as outfile:
         with open(temp_path, "r+") as infile:
             for line in infile:
+                # print("Line is: {}".format(line))
                 match = re.match(path_regex, line)
                 if match:
                     try:
                         path = Path(match.group(1), no_expand=True).fslash(with_drive=False)
                         outfile.write(line.replace(match.group(1), path))
                     except ValueError:
                         outfile.write(line)
                 else:
-                    outfile.write(line)
+                    try:
+                        outfile.write(line)
+                    except:
+                        ix.log_error("This error may occur when writing a line with non-ascii characters.")
+                        ix.log_error("It can happen when you are referencing project files that were saved on another os.")
+                        raise
 
 
 def _get_path_line_regex():
     """
     Generate a regex to help identify filepath attributes.
 
     As we scan project files to replace windows paths, we use this regex which
```

## cioclarisse/validation.py

```diff
@@ -166,15 +166,18 @@
                 missing.append(pp)
 
         if missing:
             for asset in missing:
                 self.add_warning("Missing: {}".format(asset))
 
 def run(node):
- 
+    if node.get_attribute("disable_validation").get_bool():
+        ix.log_info("Validation disabled for {}".format(node.get_name()))
+        return
+
     errors, warnings, notices = _run_validators(node)
 
     if errors:
         for error in errors:
             ix.log_error(error)
         msg = "There are some critical issues. You can't submit until you fix them."
         raise ValidationError(msg)
@@ -186,14 +189,16 @@
             msg = "Submission cancelled by user."
             raise ValidationError(msg)
     else:
         ix.log_info("There are no Notices or Warnings")
 
  
 def _run_validators(node):
+
+
     meta_warnings = set()
     validators =  [plugin(node) for plugin in Validator.plugins()]
     for validator in validators:
         msg = "Running Validator: {}".format(validator.__class__.__name__)
         ix.log_info(msg)
         try:
             validator.run("main")
```

## Comparing `cioclarisse-0.2.9.dist-info/METADATA` & `cioclarisse-0.3.1.dist-info/METADATA`

 * *Files 6% similar despite different names*

```diff
@@ -1,28 +1,29 @@
 Metadata-Version: 2.1
 Name: cioclarisse
-Version: 0.2.9
+Version: 0.3.1
 Summary: Clarisse plugin for Conductor Cloud Rendering Platform.
 Home-page: https://github.com/ConductorTechnologies/cioclarisse
 Author: conductor
 Author-email: info@conductortech.com
 License: UNKNOWN
 Platform: UNKNOWN
 Classifier: Operating System :: OS Independent
 Classifier: Programming Language :: Python
 Classifier: Topic :: Multimedia :: Graphics :: 3D Rendering
 Description-Content-Type: text/markdown
+Requires-Dist: certifi (==2019.11.28)
 Requires-Dist: ciocore (<5.0.0,>=4.0.6)
-Requires-Dist: ciopath (<1.0.0,>=0.1.4)
+Requires-Dist: ciopath (<2.0.0,>=1.0.2)
 Requires-Dist: cioseq (<1.0.0,>=0.1.13)
 Requires-Dist: ciotemplate (<1.0.0,>=0.1.2)
 
 # Conductor for Clarisse
 
-A scripted class submitter plugin for Clarisse iFX, compatible with versions 4.0 and 5.0, all service packs.
+A scripted class submitter plugin for Clarisse iFX, compatible with versions 5.0.
 
 ## Install
 
 
 **To install the latest version.**
 ```bash
 pip install --upgrade cioclarisse --target=$HOME/Conductor
@@ -90,14 +91,32 @@
 
 
 ## License
 [MIT](https://choosealicense.com/licenses/mit)
 
 ## Changelog
 
+### Version:0.3.1 -- 15 Dec 2022
+
+* Fixed a crash when closing extra environment window. [1d0163b]
+* Adds the ability to include metadata wit the submission. [18ae897]
+
+### Version:0.2.13 -- 06 Dec 2022
+
+* Use the dependency (ciopath with faster deduplication code. 
+* Fix a bug where irregular chunks with a semicolon could be interpreted as the end of command.
+
+### Version:0.2.11 -- 26 Oct 2022
+
+* Lock certifi version. [1035f41]
+
+### Version:0.2.10 -- 04 Jul 2022
+
+* Adds a checkbox to bypass all validations. [7f327e3]
+
 ### Version:0.2.9 -- 04 Jul 2022
 
 * On connect, we ensure dropdown menus have a valid selection. [a3a6ba0]
 * Remove post submission render package cleanup since it was disabled for daemon uploads anyway, and having it persist can be useful for troubleshooting. [1a08036]
 * Catch validation exceptions and when they happen, issue a validation meta-warning so as noty to block the submission. [23627c4]
 * Add a button on the submission success message box that opens a web browser at the job url on the dashboard. [e79855b]
 * Fixed a crash that appeared in Clarisse 5SP-8 that appeared when closing a Gui Window, such as the Extra Assets window. [b34e0ac]
```

